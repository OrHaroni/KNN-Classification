#ifndef DIS_H
#define DIS_H

#include <vector>
#include <cmath>
#include <iostream>
using namespace std;
/*Define the functions that will be in the distance.*/
double distanceByP(vector<double>, vector<double>, double);

double canberraDistance(vector<double>, vector<double>);

double chebyshevDistance(vector<double>, vector<double>);

#endif
